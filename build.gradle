plugins {
    id 'java-library'
}

java.toolchain.languageVersion.set(JavaLanguageVersion.of(11))

tasks.register("generateRunDirectory") {
    doLast {
        mkdir "run"
    }
}

dependencies {
    api libs.gamelauncherApi
    runtimeOnly libs.gamelauncherLwjgl
}
